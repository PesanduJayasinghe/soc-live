import { describe, it, expect, vi } from 'vitest';\nimport { render, screen, fireEvent } from '@testing-library/react';\nimport { ScavengerHuntScreen } from '../components/ScavengerHuntScreen';\nimport type { BingoSquareData } from '../types';\n\ndescribe('ScavengerHuntScreen', () => {\n  const mockItems: BingoSquareData[] = [\n    { id: 1, text: 'Finds someone who speaks 3 languages', isMarked: false, isFreeSpace: false },\n    { id: 2, text: 'Finds someone with a dog', isMarked: false, isFreeSpace: false },\n    { id: 3, text: 'Finds someone who codes', isMarked: false, isFreeSpace: false },\n    { id: 4, text: 'Finds someone who travels often', isMarked: false, isFreeSpace: false },\n    { id: 5, text: 'FREE SPACE', isMarked: true, isFreeSpace: true },\n  ];\n\n  const mockOnItemMark = vi.fn();\n  const mockOnReset = vi.fn();\n\n  describe('Rendering', () => {\n    it('should render the scavenger hunt screen', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText('SCAVENGER HUNT')).toBeInTheDocument();\n    });\n\n    it('should display all items as a list', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText('Finds someone who speaks 3 languages')).toBeInTheDocument();\n      expect(screen.getByText('Finds someone with a dog')).toBeInTheDocument();\n      expect(screen.getByText('Finds someone who codes')).toBeInTheDocument();\n    });\n\n    it('should render BACK button', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      const backButton = screen.getByText('← BACK');\n      expect(backButton).toBeInTheDocument();\n      expect(backButton).toBeEnabled();\n    });\n  });\n\n  describe('Progress Tracking', () => {\n    it('should display progress meter', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText('PROGRESS')).toBeInTheDocument();\n    });\n\n    it('should display correct item count (0 / 5)', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText('0 / 5')).toBeInTheDocument();\n    });\n\n    it('should update count when items are marked', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set([1, 2])}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText('2 / 5')).toBeInTheDocument();\n    });\n\n    it('should display progress percentage at 0%', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText('0% COMPLETE')).toBeInTheDocument();\n    });\n\n    it('should display progress percentage at 40%', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set([1, 2])}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText('40% COMPLETE')).toBeInTheDocument();\n    });\n\n    it('should display progress percentage at 100%', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set([1, 2, 3, 4, 5])}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText('100% COMPLETE')).toBeInTheDocument();\n    });\n  });\n\n  describe('Item Interaction', () => {\n    it('should call onItemMark when an item is clicked', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      const firstItem = screen.getByText('Finds someone who speaks 3 languages').closest('button');\n      fireEvent.click(firstItem!);\n\n      expect(mockOnItemMark).toHaveBeenCalledWith(1);\n    });\n\n    it('should not call onItemMark when free space is clicked', () => {\n      const mockOnItemMarkForFreeSpace = vi.fn();\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMarkForFreeSpace}\n          onReset={mockOnReset}\n        />\n      );\n\n      const freeSpaceButton = screen.getAllByRole('button').find(\n        (btn) => btn.textContent?.includes('FREE SPACE')\n      );\n\n      fireEvent.click(freeSpaceButton!);\n\n      expect(mockOnItemMarkForFreeSpace).not.toHaveBeenCalled();\n    });\n\n    it('should disable free space button', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      const freeSpaceButton = screen.getAllByRole('button').find(\n        (btn) => btn.textContent?.includes('FREE SPACE')\n      );\n\n      expect(freeSpaceButton).toBeDisabled();\n    });\n  });\n\n  describe('Completion State', () => {\n    it('should not show completion message when items are not all marked', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set([1, 2])}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.queryByText(/HUNT COMPLETE/)).not.toBeInTheDocument();\n    });\n\n    it('should show completion message when all items are marked', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set([1, 2, 3, 4, 5])}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText(/HUNT COMPLETE/)).toBeInTheDocument();\n    });\n\n    it('should display mission accomplished in footer when complete', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set([1, 2, 3, 4, 5])}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText(/MISSION ACCOMPLISHED/)).toBeInTheDocument();\n    });\n\n    it('should display remaining items count in footer', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set([1])}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText(/FIND 4 MORE ITEMS/)).toBeInTheDocument();\n    });\n  });\n\n  describe('Navigation', () => {\n    it('should call onReset when back button is clicked', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      const backButton = screen.getByText('← BACK');\n      fireEvent.click(backButton);\n\n      expect(mockOnReset).toHaveBeenCalled();\n    });\n  });\n\n  describe('Visual Feedback', () => {\n    it('should show checkmark for marked items', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set([1, 2])}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      const buttons = screen.getAllByRole('button');\n      const markedButtons = buttons.filter((btn) =>\n        btn.textContent?.includes('✓')\n      );\n\n      expect(markedButtons.length).toBeGreaterThanOrEqual(2);\n    });\n\n    it('should have different styling for marked vs unmarked items', () => {\n      const { container } = render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set([1])}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      const buttons = container.querySelectorAll('button');\n      const markedButton = Array.from(buttons).find((btn) =>\n        btn.textContent?.includes('speaks 3 languages')\n      );\n      const unmarkedButton = Array.from(buttons).find((btn) =>\n        btn.textContent?.includes('with a dog')\n      );\n\n      expect(markedButton?.className).toContain('neon-pulse');\n      expect(unmarkedButton?.className).not.toContain('neon-pulse');\n    });\n\n    it('should display item index numbers', () => {\n      render(\n        <ScavengerHuntScreen\n          items={mockItems}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText('01')).toBeInTheDocument();\n      expect(screen.getByText('02')).toBeInTheDocument();\n    });\n  });\n\n  describe('Edge Cases', () => {\n    it('should handle empty item list', () => {\n      render(\n        <ScavengerHuntScreen\n          items={[]}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText('0 / 0')).toBeInTheDocument();\n      expect(screen.getByText('100% COMPLETE')).toBeInTheDocument();\n    });\n\n    it('should handle single item', () => {\n      const singleItem = [mockItems[0]];\n      render(\n        <ScavengerHuntScreen\n          items={singleItem}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText('0 / 1')).toBeInTheDocument();\n    });\n\n    it('should handle very long item text', () => {\n      const longItem: BingoSquareData = {\n        id: 1,\n        text: 'This is a very long item description that goes on and on and might wrap to multiple lines in the UI',\n        isMarked: false,\n        isFreeSpace: false,\n      };\n\n      render(\n        <ScavengerHuntScreen\n          items={[longItem]}\n          markedItems={new Set<number>()}\n          onItemMark={mockOnItemMark}\n          onReset={mockOnReset}\n        />\n      );\n\n      expect(screen.getByText(/long item description/)).toBeInTheDocument();\n    });\n  });\n});\n